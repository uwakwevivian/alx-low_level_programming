#include "main.h"

/**
 * _isdigit -checks if numbers are 0 through 9
 * @c: char to check
 * Return: 0 or 1
 */
int _isdigit(int c)

{
	int multi = a * b;

	return multi = a * b;
			return (multi);
}
