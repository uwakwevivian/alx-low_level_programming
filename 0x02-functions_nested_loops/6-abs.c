#include "main.h"

/**
 * _abs - Number to return it's Absolute value
 * @a: Int value to be checked
 * Return: Absolute value
 */
int _abs(int x)
{
	if (x < 0)
	{
		x = -x;

		return (x);
	}
	else
	{
		return (x);
	}
}
