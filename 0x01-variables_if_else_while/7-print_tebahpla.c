#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char x = 'z';

			while (x >= 'z')
		{
			putchar(x);
			a--;
		}
		putchar('\n');
		return (0);
}

